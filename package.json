{
  "name": "can-autoplay",
  "version": "1.0.1",
  "main": "lib/index.js",
  "author": "Caio Gondim <me@caiogondim.com>",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "ava": "^0.23.0",
    "browserify": "^14.5.0",
    "puppeteer": "^0.13.0",
    "standard": "^10.0.3",
    "watchify": "^3.9.0"
  },
  "scripts": {
    "build": "browserify lib/index.js > build/can-autoplay.js --standalone canAutoplay",
    "watch": "watchify lib/index.js -o build/can-autoplay.js -v --standalone canAutoplay",
    "lint": "standard --fix 'lib/**/*.js' 'test/**/*.js'",
    "test": "yarn build && ava test/"
  },
  "dependencies": {}
}
